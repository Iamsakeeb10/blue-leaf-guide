import 'package:flutter/material.dart';

class MonthlyGoalScreen extends StatelessWidget {
  const MonthlyGoalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Monthly Goal Content"));
  }
}

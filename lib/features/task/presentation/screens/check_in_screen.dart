import 'package:flutter/material.dart';

/// ----------------------
/// Placeholder Screens
/// ----------------------
class CheckInScreen extends StatelessWidget {
  const CheckInScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Check-in Content"));
  }
}
